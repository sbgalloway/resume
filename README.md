# sambgalloway.com

Personal site — currently a placeholder page. Built with Vue 3 + Vite, deployed to GitHub Pages from `docs/`.

## Development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

Output goes to `docs/`, which is served by GitHub Pages.
